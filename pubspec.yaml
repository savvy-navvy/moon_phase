name: moon_phase
description: Flutter plugin that creates moon widgets according to the moon's phase.
version: 1.0.1+8
homepage: https://github.com/enoosoft/moon_phase

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
